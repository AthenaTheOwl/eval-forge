# Streamlit Community Cloud reads this file. streamlit_app.py reads the committed
# eval pack (YAML) and run report (JSON) directly, so it only needs streamlit +
# PyYAML. The eval_forge package has its own deps in pyproject.toml.
streamlit>=1.40
PyYAML>=6.0
